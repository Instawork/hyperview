import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  bottomSheetContainer: {
    position: 'absolute',
    width: '100%',
  },
  container: {
    left: 0,
    position: 'absolute',
    right: 0,
  },
  overflow: {
    overflow: 'hidden',
  },
  overlay: {
    bottom: 0,
    left: 0,
    position: 'absolute',
    right: 0,
    top: 0,
  },
  wrapper: {
    bottom: 0,
    left: 0,
    position: 'absolute',
    right: 0,
  },
});
