{% import 'macros.nj' as macros %}

{{ macros.description('Tapping the button below will replace the content with a copy of a hidden fragment below.') }}
{# behavior attributes can be attached to a view too #}
{{ macros.button('Press me', attributes={"action": "replace", "href":"#hidden-fragment"}) }}
<view hide="true">
  {{ macros.button('From hidden fragment', attributes={id:"hidden-fragment"}) }}
</view>
