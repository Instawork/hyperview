{% import 'macros.nj' as macros %}

{{ macros.description('Tapping the button below will replace the content with a copy of the fragment below.') }}
{# behavior attributes can be attached to a view too #}
{{ macros.button('Press me', attributes={"action": "replace", "href":"#fragment"}) }}
{{ macros.button('Fragment for Replace', attributes={id:"fragment"}) }}
