{% import 'macros.nj' as macros %}

{{ macros.description('Tapping the button below will request an XML fragment and replace the element with the response.') }}
{# behavior attributes can be attached to a view too #}
{{ macros.button('Press me', attributes={"action": "replace", "href":"/behaviors/basic/replace/once/_replace_fragment.xml"}) }}
